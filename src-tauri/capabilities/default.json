{
  "identifier": "default",
  "description": "Default permissions for ImageResizer",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
